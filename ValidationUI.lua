-- ESOtk ValidationUI - Chat output formatting
-- See ESO-657 for full implementation

ESOtk = ESOtk or {}
ESOtk.ValidationUI = {}

local ValidationUI = ESOtk.ValidationUI

--- Format and display a validation result in chat.
--- @param result table  Validation result data
function ValidationUI.DisplayResult(result)
    ESOtk.Util.Print("Validation UI not yet implemented. (ESO-657)")
end

--- Display a summary of validation results.
--- @param results table  Array of validation results
function ValidationUI.DisplaySummary(results)
    ESOtk.Util.Print("Validation summary UI not yet implemented. (ESO-657)")
end
